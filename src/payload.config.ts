// Minimal Payload config to prevent TypeScript errors
// This is disabled for the template system
export default {
  admin: {
    user: 'users',
  },
  collections: [],
  secret: 'disabled',
  db: null,
}